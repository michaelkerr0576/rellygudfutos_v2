export enum QueryStatus {
  FAILED = 'failed',
  PENDING = 'pending',
  SUCCESS = 'success',
}

// #region Photo Enum
export enum PhotoAspectRatio {
  LANDSCAPE = 'landscape',
  PORTRAIT = 'portrait',
}

export enum PhotoSortOptions {
  NEWEST = 'newest',
  OLDEST = 'oldest',
  TITLE_AZ = 'title_az',
  TITLE_ZA = 'title_za',
  RANDOM = 'random',
}
// #endregion

// #region User Enum
export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
}
// #endregion

// Photo.create(req.body)
// .then((result) => {
//   // do something with result

//   return result;
// })
// .then((user) => res.json(user))
// .catch(next);

const photosService = {};

export default photosService;
